<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="team.fzo.puppas.mini_player">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="team.fzo.puppas.mini_player.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="team.fzo.puppas.mini_player.activities.MusicListActivity"
            android:theme="@style/AppTheme.Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />

                <action android:name="musicPlayer.broadcast.SONG_SELECTED" />
            </intent-filter>
        </activity>
        <activity
            android:name="team.fzo.puppas.mini_player.activities.DetailActivity"
            android:theme="@style/AppTheme.Detail" />

        <service
            android:name="team.fzo.puppas.mini_player.service.PlayService"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>